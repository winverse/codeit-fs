# 해설: 05. Search Params 응용\n\n## 🎯 챌린지 목표\n\n`useSearchParams` 훅을 사용하여, 상품 데이터를 이름으로 검색하고 가격으로 정렬하는 응용 과제를 해결합니다. 강의에서 배운 개념을 다른 데이터 구조와 요구사항에 적용하는 방법을 연습합니다.\n\n## 📝 단계별 해설\n\n`SearchPage.jsx` 파일의 `TODO` 주석을 따라 기능을 완성합니다.\n\n### **1 & 2단계: `useSearchParams` 설정 및 파라미터 읽기**\n\n가장 먼저 `useSearchParams` 훅을 호출하고, URL로부터 `search`와 `orderBy` 파라미터 값을 읽어옵니다.\n\n```jsx\n// SearchPage.jsx (수정 전)\n// TODO 1: useSearchParams 훅을 사용해 searchParams와 setSearchParams를 가져오세요.\n// TODO 2: URL로부터 \'search\'와 \'orderBy\' 쿼리 파라미터 값을 가져오세요.\nconst searchTerm = null;\nconst orderBy = null;\n```\n\n강의 내용과 동일하게 `useSearchParams`를 호출하고, `searchParams.get()` 메서드를 사용하여 각 파라미터의 값을 가져옵니다.\n\n```jsx\n// SearchPage.jsx (수정 후)\nconst [searchParams, setSearchParams] = useSearchParams();\nconst searchTerm = searchParams.get(\'search\');\nconst orderBy = searchParams.get(\'orderBy\');\n```\n\n### **3단계: 검색 기능 구현 (`handleSubmit`)**\n\n검색 폼을 제출했을 때, `search` 쿼리 파라미터를 업데이트합니다.\n\n```jsx\n// SearchPage.jsx (수정 전)\n// TODO 3: form의 \'search\' input 값을 가져와 search 쿼리 파라미터를 업데이트하세요.\n```\n\n`FormData`를 이용해 `name=\"search\"`인 input의 값을 가져오고, `setSearchParams`를 사용하여 URL을 업데이트합니다.\n\n```jsx\n// SearchPage.jsx (수정 후)\nconst formData = new FormData(e.target);\nconst newSearchTerm = formData.get(\'search\');\nsetSearchParams({ search: newSearchTerm || \'\' });\n```\n\n### **4단계: 정렬 기능 구현 (`handleSort`)**\n\n정렬 버튼을 클릭했을 때, 기존 `search` 파라미터는 유지하면서 `orderBy` 파라미터를 업데이트합니다.\n\n```jsx\n// SearchPage.jsx (수정 전)\n// TODO 4: 기존 \'search\' 파라미터는 유지하면서 \'orderBy\' 파라미터를 업데이트하세요.\n```\n\n`setSearchParams`에 객체를 전달할 때, 기존 `searchTerm` 값을 함께 넣어주어야 검색 상태가 유지됩니다.\n\n```jsx\n// SearchPage.jsx (수정 후)\nsetSearchParams({ search: searchTerm || \'\', orderBy: sortType });\n```\n\n### **5단계: 데이터 필터링 및 정렬 로직 구현**\n\n`searchTerm`과 `orderBy` 값에 따라 `products` 배열을 처리합니다.\n\n```jsx\n// SearchPage.jsx (수정 전)\n// TODO 5: searchTerm과 orderBy 값에 따라 products 배열을 필터링하고 정렬하는 로직을 구현하세요.\nconst filteredProducts = [];\n```\n\n먼저 `filter`로 `name`에 `searchTerm`이 포함된 상품만 거르고, 그 결과를 `sort`를 이용해 `price` 기준으로 정렬합니다.\n\n```jsx\n// SearchPage.jsx (수정 후)\nconst filteredProducts = (searchTerm\n  ? products.filter((p) =>\n      p.name.toLowerCase().includes(searchTerm.toLowerCase())\n    )\n  : []\n).sort((a, b) => {\n  if (orderBy === \'price-asc\') return a.price - b.price;\n  if (orderBy === \'price-desc\') return b.price - a.price;\n  return 0;\n});\n```\n\n### **6단계: 조건부 렌더링**\n\n검색어 존재 여부와 검색 결과 유무에 따라 다른 UI를 보여줍니다.\n\n```jsx\n// SearchPage.jsx (수정 전)\n{/* TODO 6: searchTerm의 존재 여부와 filteredProducts의 길이에 따라 다른 UI를 보여주세요. */}\n<p>검색어를 입력하여 상품을 찾아보세요.</p>\n```\n\n강의에서 배운 삼항 연산자 중첩 패턴을 응용하여 구현합니다.\n\n```jsx\n// SearchPage.jsx (수정 후)\n{searchTerm ? (\n  filteredProducts.length > 0 ? (\n    <ul>\n      {filteredProducts.map((p) => (\n        <li key={p.id}>\n          {p.name} (가격: {p.price.toLocaleString()}원)\n        </li>\n      ))}\n    </ul>\n  ) : (\n    <p>\`{searchTerm}\`에 대한 검색 결과가 없습니다.</p>\n  )\n) : (\n  <p>검색어를 입력하여 상품을 찾아보세요.</p>\n)}\n```\n\n## 🎉 최종 완성 코드\n\n모든 `TODO`를 해결한 최종 코드는 다음과 같습니다.\n\n```jsx\n// 전체 코드는 solution 폴더를 참고하세요.\n```\n\n이번 챌린지를 통해 `useSearchParams`를 활용하여, 실제 쇼핑몰의 검색/필터 기능과 유사한 기능을 구현하는 방법을 연습했습니다.\n